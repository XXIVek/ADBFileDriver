# Драйвер ADB для 1С:Предприятие 8.3

## Описание
Внешняя компонента для обмена данными между 1С:Предприятие 8.3 и Android устройствами через ADB (Android Debug Bridge).

## Архитектура
```
┌─────────────────────────────────────────────────────────┐
│                    1С:Предприятие 8.3                    │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Драйвер = Новый("AddIn.ADBFileDriver.ADBFileDriver") │ │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              ADBFileDriver_Win64.dll                     │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────┐  │
│  │  Свойства   │  │   Методы     │  │   Логирование │  │
│  │  ·Версия    │  │ ·Подключить  │  │               │  │
│  │ ·Статус     │  │ ·Отключить   │  │               │  │
│  │ ·DeviceCount│  │ ·Enumerate   │  │               │  │
│  └─────────────┘  └──────────────┘  └───────────────┘  │
└────────────────────────┬────────────────────────────────┘
                         │ LoadLibrary
┌────────────────────────▼────────────────────────────────┐
│              AdbWinUsbApi.dll                            │
│  ·AdbEnumInterfaces    ·AdbNextInterface               │
│  ·AdbCreateInterfaceByName                         │
│  ·AdbGetSerialNumber    ·AdbGetUsbDeviceDescriptor    │
│  ·AdbCloseHandle                                     │
└─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│              USB ADB Device (GUID)                       │
│  {F72FE0D4-CBCB-407d-8814-9ED673D0DD6B}               │
└─────────────────────────────────────────────────────────┘
```

## Установка
1. Скопируйте `ADBFileDriver_Win64.dll` в нужную директорию
2. Скопируйте `bundle/MANIFEST.xml` в папку `bundle/` рядом с DLL
3. Убедитесь что `adb/AdbWinUsbApi.dll` доступен (в папке adb или в системе)

## Подключение в 1С

```bsl
// Подключение компоненты
Результат = ПодключитьВнешнююКомпоненту(
    "путь/к/ADBFileDriver_Win64.dll",
    "ADBFileDriver",
    ТипВнешнейКомпоненты.Native
);

Если Результат Тогда
    Драйвер = Новый("AddIn.ADBFileDriver.ADBFileDriver");
КонецЕсли;
```

## Свойства

| Свойство | Тип | Описание |
|----------|-----|----------|
| `Версия` | Строка | Номер версии компоненты ("1.0.0.1") |
| `ВключитьЛогирование` | Булево | true/false включение логирования |
| `ПутьДляФайлаЛогирования` | Строка | Путь к файлу лога (по умолчанию: %TEMP%\ADBFileDriver.log) |
| `Статус` | Строка | "Не подключен" / "Подключено" / "Ошибка: ..." |
| `КоличествоУстройств` | Число | Количество найденных USB ADB устройств |

## Методы

### Подключить()
Подключает компоненту (устанавливает статус "Подключено").

**Возвращаемое значение:** Boolean - true при успехе

### Отключить()
Отключает компоненту (устанавливает статус "Не подключен").

**Возвращаемое значение:** Boolean - true при успехе

### ПеречислитьУстройства()
Перечисляет все подключенные Android устройства через AdbWinUsbApi.dll.

**Возвращаемое значение:** Строка (JSON формат)

**Пример JSON:**
```json
[
  ["serial":"RF8T5XXXXXXXX","vendorId":0x04E8,"productId":0x6860]
]
```

### ПолучитьИнформациюОУстройстве(Индекс)
Возвращает информацию об устройстве по индексу.

**Параметры:**
- `Индекс` (Число) - индекс устройства (0-based)

**Возвращаемое значение:** Строка (JSON формат с информацией об устройстве)

## Пример использования в 1С

```bsl
// Подключение
Результат = ПодключитьВнешнююКомпоненту(
    "путь/к/ADBFileDriver_Win64.dll",
    "ADBFileDriver",
    ТипВнешнейКомпоненты.Native
);

Если Результат Тогда
    Драйвер = Новый("AddIn.ADBFileDriver.ADBFileDriver");
    
    // Проверка версии
    Сообщить(Драйвер.Версия); // "1.0.0.1"
    
    // Включение логирования
    Драйвер.ВключитьЛогирование = Истина;
    Драйвер.ПутьДляФайлаЛогирования = "C:\Logs\ADBFileDriver.log";
    
    // Подключение
    Драйвер.Подключить();
    Сообщить(Драйвер.Статус); // "Подключено"
    
    // Перечисление устройств
    JSON = Драйвер.ПеречислитьУстройства();
    Сообщить(JSON);
    Сообщить(Драйвер.КоличествоУстройств);
    
    // Отключение
    Драйвер.Отключить();
    Сообщить(Драйвер.Статус); // "Не подключен"
    
    // Безопасное уничтожение
    Драйвер = Неопределено;
КонецЕсли;
```

## Сборка

### Требования
- CMake 3.10+
- Visual Studio 2022
- Windows SDK

### Команда сборки
```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

### Результат
- `1C/Release/Win64/ADBFileDriver_Win64.dll` - DLL компоненты
- `1C/Release/Win64/bundle/MANIFEST.xml` - манифест для 1С

## История версий

### v1.0.0.1
- Добавлена поддержка логирования
- Добавлены методы Подключить/Отключить
- Добавлено свойство Статус
- Исправлен краш при уничтожении объекта (использование Windows API вместо C++ STL)
- Добавлено перечисление USB устройств через AdbWinUsbApi.dll

### v1.0.0.0 (MVP)
- Базовая поддержка свойств (Версия, ВключитьЛогирование, ПутьДляФайлаЛогирования)
- Подключение/отключение
- Безопасная работа с памятью

## GitHub
https://github.com/XXIVek/ADBFileDriver