Зафиксируй в описании что должно выводиться при логировании в файл и реализую в коде.
1. Факт подключения компоненты.
2. Состояние Свойств: Version, КаталогADB, EnableLog, LogPath.
3. Выполнение метода  ПеречислитьУстройства(). Вывести результат.
4. Выполнение метода Подключить(ИмяУстройства). Вывести результат и имя подключенного устройства.
5. Вывести Свойства: Status, DeviceCount.
6. Выполнение метода СписокФайлов(Путь). Вывести результат и путь.
7. Вывести Свойства: FileCount.
8. Выполнение метода ЗагрузитьФайл(ИмяФайла, Содержание). Вывести результат, ИмяФайла, Содержание.
9. Выполнение метода СкачатьФайл(ИмяФайла). Вывести результат, ИмяФайла.
10.  Выполнение метода УдалитьФайл(ИмяФайла). Вывести результат, ИмяФайла.
11. Факт отключения компонеты. Результат закрытия сервера ADB.